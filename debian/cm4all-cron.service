[Unit]
Description=CM4all Cron
After=network.target

[Service]
Type=notify
ExecStart=/usr/sbin/cm4all-cron
KillMode=process
ExecReload=/bin/kill -HUP $MAINPID
Restart=on-failure

CPUSchedulingPolicy=batch
RestrictRealtime=yes
TimerSlackNSec=1s

[Install]
WantedBy=multi-user.target
